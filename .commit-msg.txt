Address audit: POST review, security headers, provider errors, art-cache hygiene

- Changed /api/reading/review from GET query params to POST with JSON body so
  birth date/time/placeId no longer appear in URLs, logs, or Referer headers.
- Added HSTS, CSP, X-Frame-Options, X-Content-Type-Options, and
  Referrer-Policy via next.config.mjs headers().
- Removed upstream response bodies from live provider error messages;
  generic HTTP status is returned instead.
- Removed duplicate X-Api-Key geocoding header; only Authorization is sent.
- Removed tracked data/art-cache/ JSON files from git (kept on disk).
- Updated docs/data-license.md to recommend out-of-repo quarantine.
- Adjusted tests for POST review and single-header geocoding.

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
